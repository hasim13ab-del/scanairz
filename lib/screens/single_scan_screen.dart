import 'package:flutter/material.dart';

class SingleScanScreen extends StatelessWidget {
  const SingleScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Single Scan Screen'),
      ),
    );
  }
}
