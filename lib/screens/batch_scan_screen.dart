import 'package:flutter/material.dart';

class BatchScanScreen extends StatelessWidget {
  const BatchScanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Batch Scan Screen'),
      ),
    );
  }
}
