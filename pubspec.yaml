name: scanairz
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobile_scanner: ^4.0.1
  permission_handler: ^11.3.1
  connectivity_plus: ^6.0.3
  intl: ^0.19.0
  share_plus: ^7.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  dio: ^5.0.0
  shared_preferences: ^2.5.3
  audioplayers: ^6.5.1
  csv: ^6.0.0
  vibration: ^3.1.3
  provider: ^6.1.5+1
  url_launcher: ^6.1.14
  firebase_core: ^2.27.2
  firebase_remote_config: ^4.3.20

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
    - assets/images/
